{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/product.schema.json",
  "title": "PGVector Langchain Wise Agent Vector DB",
  "description": "A LangChainWiseAgentVectorDB implementation that makes use of a LangChain PGVector database",
  "type": "object",
  "properties": {
    "connection_string": {
      "type": "string",
      "description": "The connection string to the Neo4j database"
    },
    "embedding_model_name": {
      "type": "string",
      "description": "The optional name of the embedding model to use",
      "default": "all-mpnet-base-v2"
    }
  },
  "required": [
    "connection_string"
  ]
}
